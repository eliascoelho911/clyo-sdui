package com.clyo.android

class ClyoEngine {
//    fun createScreen(context: Context, json: LayoutJson): Layout<*> {
//        val layout = layoutProvider.provide(context, json.type)
//
//        json.content.forEach { widgetJson ->
//            layout.add(Widget())
//        }
//    }
}