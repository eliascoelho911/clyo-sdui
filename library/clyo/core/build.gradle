apply plugin: 'org.jetbrains.kotlin.jvm'
apply plugin: 'kotlinx-serialization'

apply from: "$rootDir/plugins/kotlin.gradle"

dependencies {
    // LocalLibs
    implementation project(LocalLibs.clyoCommon)

    implementation Libs.kotlinReflect
}